  <script>
/*
$(document).ready(function() { 
$(".jc-tab-content-btn-link-next").click(function(){
    console.log('picked up');
    var tab = $(this).parents('.jc-tab-pane');
    var tabNumber = tab.attr('data-w-tab');
    console.log(tabNumber);

    if( tabNumber == "Tab 1" ){
      $("a[data-w-tab='Tab 2']")[0].click();
    }
    else if( tabNumber == "Tab 2" ){
      
      $(".jc-tabs-menu-link[data-w-tab='Tab 3']")[0].click();

    }
    else if( tabNumber == "Tab 3" ){
      $("a[data-w-tab='Tab 1']")[0].click();
    } else {
    console.log('no tab following');
    }
});
$(".jc-tab-content-btn-link-prev").click(function(){
    var tab = $(this).parents('.jc-tab-pane');
    var tabNumber = tab.attr('data-w-tab');
    if( tabNumber == "Tab 1" ){
      $("a[data-w-tab='Tab 3']")[0].click();
    }
    else if( tabNumber == "Tab 2" ){
      $("a[data-w-tab='Tab 1']")[0].click();
    }
    else if( tabNumber == "Tab 3" ){
      $("a[data-w-tab='Tab 2']")[0].click();
    } else {
    console.log('no tab following');
    }
});
 });
*/
</script>